# Binder-Organizer
A Python/Tkinter desktop application for organizing physical collections into customizable binders. It allows you to import data from CSV files, configure the binder size (rows/columns), track progress with clicks, search by name or number, and save data across multiple independent collections.
